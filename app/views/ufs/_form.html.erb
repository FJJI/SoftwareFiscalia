<%= form_with(model: uf, local: true) do |form| %>
  <% if uf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(uf.errors.count, "error") %> prohibited this uf from being saved:</h2>

      <ul>
        <% uf.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fiscals_id , class:"pl-2 font-weight-bold" %>
    <%= form.collection_select :fiscals_id, Fiscal.all,:id, :name %>
  </div>

  <div class="field">
    <%= form.label :user_id , class:"pl-2 font-weight-bold" %>
    <%= form.collection_select :user_id, User.all,:id, :name %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
